// Leer entrada -> Procesar -> Imprimir salida

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;         // Leer datos de entrada
    int suma = a + b;      // Procesar el problema
    cout << suma << endl;  // Imprimir la salida
    return 0;
}